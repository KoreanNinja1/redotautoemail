# 周末计划 · 2026-06-28 / 29

> **原则：先定「发什么」（PDF）→ 再定「记在哪」（Notion 状态表）→ 最后定「怎么跑」（流程 + 计划）。**  
> 流程和 todo 机制都依赖前两步；不要反过来先排 todo、PDF 还没定稿。

---

## 推荐顺序（按这个做）

| 顺序 | 做什么 | 哪天 | 为什么在这 |
|:---:|--------|------|------------|
| **1** | **KOL + Affiliate PDF → 2–3 页** | 周六上午 | 外联/onboard 都靠这份材料；先缩短、定稿，后面流程才有锚点 |
| **2** | **Notion 状态表搭好** | 周六下午 | 外联 + onboard 一张（或关联）表，你实时改 status；Agent 读导出 + `creator-data` 帮你排计划 |
| **3** | **外联流程 & 回复效率** | 周日上午 | PDF 定了才知道 Lark/Gmail 发什么；Notion 定了才知道 status 怎么流转 |
| **4** | **Onboard 后运营**（guidelines、催稿、AI 查发稿） | 周日下午前半 | 依赖 PDF 结构 + Notion 字段；活动 guidelines 是 PDF 之外的短文档 |
| **5** | **每日 todo 机制 + 未来计划** | 周日下午后半 | 前面都清楚后，再用 **`/grilling` A + D** 排下周；数据源 = Notion 导出 + KOL csv |

---

## 周六 Checklist

### 1️⃣ PDF 缩到 2–3 页（最先做）

- [ ] **`/grilling` → C**：定 KOL / Affiliate 各留什么、删什么
- [x] KOL：**`RedotPay_KOL_v2`** — 3 页结构已定稿（见 `kol-outreach/RedotPay_KOL_v2.md`）
- [x] Affiliate：**`RedotPay_Affiliate_v2`** — 3 页结构已定稿（见 `guidebooks/RedotPay_Affiliate_v2.md`）
- [x] 导出路径已记录：`~/Downloads/RedotPay_KOL.pdf`、`~/Downloads/RedotPay_Affiliate.pdf`（佣金、合规句仍需人工终审）
- [ ] 和活动 guidelines 分工写清楚：**PDF = 长期 onboarding 包**；**活动 = 单独短 md**（不进厚 PDF）

| 文档 | 必留 | 可删 |
|------|------|------|
| KOL | personalize + 固定正文 + CTA + follow-up | 长案例、多版本 |
| Affiliate | 产品、佣金、步骤、支持 | 品牌长文、重复 FAQ |

---

### 2️⃣ Notion 状态表（PDF 定稿后再搭）

**目标：** 你 **实时更新 status**；Agent 用 **Notion 导出 + `creator-data/`** 一起做 **`/grilling` 排计划**。

- [ ] 建 Notion 库（或两个关联库），最少字段见下
- [ ] 把现有 outreach / onboard 人 **迁一批进去**（不用一次填完）
- [ ] 在 `plans/onboard-运营同步.md` 写上 **Notion 链接** + 每周导出方式
- [ ] 试一次：导出 csv → 发给 Agent → 「结合 `all_ready.csv` 排下周外联 5 条」

**建议一张主表（或 Outreach + Onboard 两表关联）：**

| 字段 | 用途 |
|------|------|
| `name` | 博主名 |
| `channel_url` | YouTube / IG / TikTok |
| `country` | IN / BR / … |
| `stage` | `candidate` · `contacted` · `replied` · `onboarded` · `posted` · `churned` |
| `ras_score` | 来自 `/rasme` 或手填 |
| `email` | |
| `outreach_date` / `onboard_date` | |
| `last_post_date` | 你或 AI 更新 |
| `current_campaign` | 活动名 |
| `status_notes` | 自由备注 |
| `next_action` | 你或 Agent 建议的下一步 |
| `source` | nox / kol_intelligence / 手工 |

**Agent 怎么用 Notion + KOL 数据：**

```text
我导出了 Notion [贴 csv 或路径]，
再结合 creator-data/queues/batches/results/all_ready.csv，
/grilling A — 排下周每日 todo，外联和 onboard 分开，每天最多 5 条
```

- **你维护：** Notion 里改 stage、日期、备注（实时）
- **仓库维护：** `creator-data/` 候选池、RAS、blacklist（脚本 `/noxinme` `/rasme`）
- **Agent：** 读两边 → 去重、排优先级、出未发稿 list（不自动发 Lark）

---

## 周日 Checklist

### 3️⃣ 外联流程 & 回复效率

- [ ] **`/grilling` → B**
- [ ] 画清：ready → Lark → Gmail → **Notion 改 stage**（不再只靠 xlsx 或本地表）
- [ ] 产出 **≥3 条** 本周可落地改进（模板固定段、follow-up 7+14 天谁提醒、与 Notion 同步节奏）
- [ ] 确认：`sync-outreach-from-xlsx` 是否仍需要，或逐步以 Notion 为准

**Grill 提纲：**

1. 一封邮件现在几步、几分钟？
2. 哪些用 `/rasme` + `by_country`？
3. PDF v2 定稿后，Lark 拼接还要改什么？

---

### 4️⃣ Onboard 后运营

> onboard 之后：发稿、活动、guidelines、催稿 — 仍和你有关。  
> **Notion `stage=onboarded` 之后** 的字段和规则在这步定。

- [ ] 活动 **guidelines 模板**（1 页）：活动名 / 时间 / 素材 / 必提 / 禁提 / deadline → 存 `plans/activities/`
- [ ] 定 **未发稿定义**（例：onboard ≥14 天无相关帖；活动 deadline 前 3 天仍无稿）
- [ ] **`/grilling` → E**：未发稿 list 规则 + AI 查频道怎么说
- [ ] 更新 `plans/onboard-运营同步.md`（Notion 链接、本周活动、提效目标）

**AI 分工（周末只定规则，下周试点）：**

| 你提供 | Agent 做 |
|--------|----------|
| Notion 导出（onboarded 的人） | 谁 90 天内没发过 RedotPay 相关内容 |
| 「相关内容」定义 | 出 `未发稿 list.csv`，标 `needs_manual_check` |
| 你确认 | 催稿话术建议；**不自动发** |

---

### 5️⃣ 每日 todo 机制 + 未来计划（最后做）

- [ ] **`/grilling` → A**：用 Notion 导出 + KOL ready 名单 → 更新 `本周每日计划.md`（7/2 起至少 3 天）
- [ ] **`/grilling` → D**：确认 Q3 阶段 0→3；同步 `RedotPay_KOL_Referral_BD_AI_提效计划.md` 如需要
- [ ] 定 **每周一** 节奏：更 Notion → 导出 → Agent grill → 写 `本周每日计划.md` + `onboard-运营同步.md`

**长期（跑通 2 周后再自动化）：**

- 周一：Notion 导出 + **`/grilling` A**
- 周五：Notion onboard 视图 + AI 未发稿 list
- 外联候选仍走 **`/noxinme` → `/rasme` → 合格进 Notion `candidate`**

---

## ④ 未来计划（Q3 摘要）

| 阶段 | 重点 |
|------|------|
| **0 · 7 上** | PDF 2–3 页 · Notion 表 · 外联手工环 · onboard 字段 |
| **1 · 7 下** | 5–10 封/天 · 每周 Notion+Agent 排 todo · 活动 guidelines 按场归档 |
| **2 · 8 月** | 外联半自动队列 · 未发稿 list 脚本化 · Notion 与 csv 定期对齐 |
| **3 · Q3** | 南美加量 · 开卡 + 发稿率一起看 |

---

## 周末结束检查（按顺序打勾）

- [ ] **1** KOL **v2 三页** 已出（`RedotPay_KOL.pdf`）· Affiliate **2–3 页** 大纲或 v2 稿
- [ ] **2** Notion 表已建、链接写入 `onboard-运营同步.md`、试过一次导出给 Agent
- [ ] **3** 外联流程改进 **≥3 条**（含 Notion 更新节点）
- [ ] **4** guidelines 模板 + 未发稿定义 + **`/grilling` E** 过一遍
- [ ] **5** `本周每日计划.md` 有 7/2 起任务 · 未来计划确认

---

## 快捷命令

```text
/grilling   →  C PDF（先做）· 建表后再 B/E · 最后 A/D
/noxinme    →  搜新博主 → 进 Notion candidate
/rasme      →  打分 → ready 同步进 Notion 或 csv
```

- Notion + Agent：`plans/onboard-运营同步.md`
- KOL 数据：`creator-data/README.md`
- 速查：`plans/SKILLS速查手册.md`
