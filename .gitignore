# Secrets — never commit
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
Thumbs.db

# Node
node_modules/

# Large media (use RedotPay-KOL-BD.zip for full assets)
videos/Redotpay.mp4

# Local-only VS Code overrides (optional)
.vscode/*.local.json

# Temp xlsx unpack dirs (sync-outreach-from-xlsx)
creator-data/.tmp_626/
creator-data/.tmp_outreach_sync/
